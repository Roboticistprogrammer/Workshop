# Project 3 Multiprocessing Vision Workers

Implement a small vision app that captures a live camera (or video file) stream, applies a heavy blur to each frame in parallel worker threads, overlays simple timing stats, and lets you toggle between single-threaded and multi-threaded processing to see the performance difference.
